// C program to show functioncall and definition
#include <stdio.h>
// Function that takes two parameters a and b as inputs and
// returns their sum
int sum(int a, int b)
{
return a + b;
}
// Driver code
int main()
{
// Calling sum function and storing its value in add //variable
printf("value of a,b")
scanf("%d%d",)
int add = sum(a, b);
printf("Sum is: %d", add);
return 0;
}