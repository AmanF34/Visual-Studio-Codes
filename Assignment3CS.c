#include <stdio.h>
int main() {
    float price, taxrate;
    printf("Enter the price and the tax rate(as a percentage): ");
    scanf("%f %f", &price, *taxRate)

    float totalcost = price + (Prise * taxrate / 100);
    printf("Total cost after tax: %.2f\n", totalcost);
    
    return 0;
}